<%= form_with(model: dateofinfection, local: true) do |form| %>
  <% if dateofinfection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dateofinfection.errors.count, "error") %> prohibited this dateofinfection from being saved:</h2>

      <ul>
        <% dateofinfection.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :dateofinfection %>
    <%= form.datetime_select :dateofinfection %>
  </div>

  <div class="field">
    <%= form.label :animal_id %>
    <%= form.text_field :animal_id %>
  </div>

  <div class="field">
    <%= form.label :disease_id %>
    <%= form.text_field :disease_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
